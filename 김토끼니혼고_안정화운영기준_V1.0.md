# 안정화 및 운영 기준

## 고정 회귀 세트

- 대표 검색어: `오바상`, `괴물`, `오니`, `지칸`, `아이쇼`, `토모다치`, `히로우`, `키쿠`
- 단일 기준 파일: `tests/search-regression-cases.json`
- 실행 명령: `node scripts/run-search-regression.mjs`

## 자동 통과 기준

- 검색어별 최상위 핵심 표현과 검색 유형이 유지되어야 한다.
- 필수 검색 결과, 연관 그룹, 연관 구성원이 모두 존재해야 한다.
- 같은 발음과 뜻의 검색 결과 또는 팝오버 구성원이 중복되면 실패한다.
- 같은 관계 수준에서는 짧은 기본형·핵심 표현이 긴 문장보다 먼저 나와야 한다.
- 일반 라벨 `연관`이 대표 관계 이름으로 다시 나타나면 실패한다.
- 검색·연관 요청은 각각 3초 초과 시 경고하고 10초를 넘으면 실패한다.

## 신규 데이터 적용 절차

1. 데이터 변경 전 회귀검사를 실행해 기준 상태를 확인한다.
2. 신규 데이터를 반영하고 변경 영역의 표적 검사를 수행한다.
3. 대표 검색 회귀검사를 다시 실행한다.
4. 8개가 모두 통과할 때만 전체 테스트와 배포를 진행한다.
5. 의도된 학습 기준 변경으로 실패했다면 사례를 검토한 뒤 계약과 보고서를 함께 수정한다.

## 운영 판단

- 결과 건수는 현재값과 완전히 같을 필요가 없다. 검색어별 최소·최대 범위 안에서 핵심 결과가 유지되면 통과한다.
- 검색 결과가 늘어 허용 상한을 넘으면 자동으로 상한을 높이지 않고, 직접 관련성이 있는 신규 결과인지 먼저 검토한다.
- 회귀 계약은 화면 코드, DB ID, 내부 생성 순서가 아니라 사용자가 실제로 보는 뜻·발음·관계명을 기준으로 한다.
- 내부 ID를 고정하지 않아 데이터 재적재 후에도 같은 기준을 적용할 수 있다.

## 확정 시점 기준값

- 대표 검색어: 8/8 통과
- 필수 연관 그룹: 11/11 통과
- 필수 연관 구성원: 18/18 통과
- 중복·일반 `연관` 라벨 실패: 0건
- DB 내부 일괄 측정: 검색 8회 평균 약 571ms, 연관 조회 약 74ms, 전체 약 4.65초
