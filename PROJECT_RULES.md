# 김토끼니혼고 프로젝트 규칙

## 신규 데이터 추가 방식

- 신규 데이터는 동일한 김토끼니혼고 프로젝트 안의 채팅에서 사용자가 추출·추가를 요청하는 방식으로 진행한다.
- Excel 업로드·입력 양식·일괄 Import 방식은 보류한다.
- 사용자가 명시적으로 방식을 변경하기 전에는 Excel 업로드 방식을 다음 작업으로 제안하거나 다시 선택하도록 질문하지 않는다.
- 채팅에서 새로 생성하거나 수집한 데이터는 기존 원본 데이터와 출처를 구분하여 관리한다.
- 사용자가 `다음` 또는 `계속`이라고 하면 신규 데이터 입력 방식에 관한 질문을 반복하지 않고, 확정된 다음 작업을 진행한다. 확정된 작업이 없으면 그 사실과 선택 가능한 실제 작업만 간단히 안내한다.

## 화면 및 공개 방식

- 앱은 별도 요청이 없는 한 로그인 없이 1인용으로 사용하며, `ACTIVE` 데이터는 모두 노출한다.
- 검색 목록은 한국어 뜻을 먼저, 한글 발음을 다음에 표시한다. 정확 일치는 내부 정렬에만 사용하고 별도 배지나 설명을 노출하지 않는다.
- 힌트·비슷·반대·활용·참조·대화 등 연결 데이터가 있으면 상세 화면을 열기 전 목록에서 버튼으로 확인할 수 있게 한다.
- 힌트·참조·비슷·반대처럼 짧은 연결 정보는 작은 팝오버를 우선 사용하며, 연결 정보가 없다는 별도 카드는 표시하지 않는다.
- Story는 `주제별`, `엣지`, `기타` 탭을 이 순서로 제공한다. 엣지 원문은 Story 전용으로 유지하며 외부 검색 연결이나 표현 검증을 하지 않는다.
- Story 본문에서는 화면의 뒤로 버튼과 기기의 뒤로가기가 모두 기존 목록 위치로 돌아가고, 목록에서 기기의 뒤로가기를 누르면 Story를 닫는다.
- 목록 위치는 상세·팝오버를 열고 닫아도 유지하며, 상세 화면 안에서 연결 표현으로 이동한 경우 이전 표현으로 돌아갈 수 있게 한다.
- 최근 본 표현·복습 목록·즐겨찾기에서 상세를 닫으면 열었던 학습 목록의 위치로 돌아온다. 학습 목록에서 기기의 뒤로가기는 목록을 닫고, 복습하기 중에는 복습 목록으로 돌아간 뒤 다음 뒤로가기로 닫는다.
- 상단 좌측 삼선 메뉴에서 `최근 본 표현`, `복습 목록`, `즐겨찾기`, `복습하기`를 제공한다. 최근 본 표현은 상세 화면을 연 표현만 중복 없이 최신순 30개까지 기록한다.
- `복습 목록`은 기존에 기각한 일반 저장 기능과 구분한다. 사용자가 기억나지 않는 단어·문장을 체크해 복습 대상으로 관리하는 기능이며, 검색 목록과 상세 화면에서 체크하거나 해제할 수 있다.
- `복습하기`는 복습 목록의 표현을 무작위 순서로 보여주고 `한국어 뜻 → 한글 발음 공개 → 기억함/다시 보기` 순서로 진행한다. `기억함`을 누르면 즉시 복습 목록에서 제외하고, `다시 보기`는 다음 회차에 재출제한다.
- `즐겨찾기`는 복습 목록과 독립적으로 관리한다. 검색 목록·상세에서 별표로 추가/해제하고 메뉴에서 모아 볼 수 있다. `기억함`은 즐겨찾기에 영향을 주지 않는다.
- 최근 기록·복습 목록·즐겨찾기는 로그인이나 서버 저장 없이 현재 브라우저 기기에만 저장한다.

## 안정화 및 운영 검사

- 데이터·검색·연관 관계를 변경한 뒤에는 `node scripts/run-search-regression.mjs`를 실행한다.
- 대표 검색어는 `오바상`, `괴물`, `오니`, `지칸`, `아이쇼`, `토모다치`, `히로우`, `키쿠` 8개로 고정한다.
- 자동검사는 최상위 핵심 표현, 필수 검색 결과, 필수 연관 그룹과 구성원, 화면 중복, 연관그룹의 역할·명시 순서, 요청 시간을 확인한다.
- 연관그룹 정렬은 관리자가 지정한 `display_order`를 최우선한다. 명시 순서가 없을 때 `member_role`·`association_level`로 정한 대표성, 검색 관련도·사용 빈도를 차례로 사용한다. 표현 길이는 앞 기준이 모두 같을 때만 마지막 보조 기준으로 사용한다.
- 기본형에서 활용형·확장형으로 이동하는 그룹에서는 대표 표현과 기본형을 먼저 보여줄 수 있지만, 모든 예문을 무조건 짧은 순서로 정렬하지 않는다.
- 예문 목록은 의미 일치도, 대표성, 실제 회화 활용도, 출처의 맥락 순서를 우선하며 길이만으로 순서를 결정하지 않는다.
- 긴 표현이 짧은 표현보다 먼저 있다는 사실만으로는 검색 오류나 회귀 실패로 판정하지 않는다. 대표 표현 누락, 명시 순서 위반, 핵심 표현이 제한 노출 범위 밖으로 밀리는 경우에만 정렬 문제로 판정한다.
- 검색 결과 건수는 현재값 자체를 고정하지 않고 검색어별 허용 상한만 둔다. 신규 표현이 추가되어도 핵심 결과와 관계가 유지되면 통과할 수 있어야 한다.
- 회귀 실패 시 신규 데이터 반영과 배포를 중단하고 원인을 수정한다. 의도된 학습 기준 변경일 때만 근거를 보고서에 남기고 회귀 계약을 함께 수정한다.
- 대표 검색어·통과 조건은 `tests/search-regression-cases.json`을 단일 기준으로 사용하며 코드와 문서에 별도 복제하지 않는다.
