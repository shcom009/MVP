# 관계 표현 짧은 순 우선 점검

## 전체 점검

- 여러 항목이 있는 활용·비슷반대 등 관계 묶음 208개를 기계적으로 비교했다.
- 기존 저장 순서와 `기본형 → 짧은 표현 → 긴 문장` 순서가 다른 묶음은 123개, 해당 항목은 421개였다.
- 이 중 활용 묶음은 70개, 비슷·반대 등 기타 관계 묶음은 53개였다.

## 처리 내용

- 검색 결과의 작은 팝오버와 상세 화면 관계 패널에 공통 정렬을 적용했다.
- 현재 표현이 활용형이면 원형을 첫 번째로 두고, 나머지는 발음 길이가 짧은 순으로 표시한다.
- 길이가 같으면 띄어쓰기가 없는 표현, 기존 `display_order`, 표현 ID 순으로 안정적으로 정렬한다.
- DB의 관계·원문·출처는 변경하지 않았다.

## 변경 결과

- 짧은 표현 우선 적용 대상: 0개 묶음 → 123개 묶음
- 정렬 대상 항목: 421개
- 정렬 규칙 적용 후 남은 순서 위반: 0개
